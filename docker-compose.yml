version: '3'
services:
    assembly:
        build:
            context: ./Assembly
            dockerfile: Dockerfile
        volumes:
            - './CommonFiles:/usr/src/app/boxfuse-sample-java-war-hello/target'
    prod:
        build:
            context: ./Production
            dockerfile: Dockerfile
        volumes:
            - './CommonFiles:/var/lib/tomcat8/webapps'